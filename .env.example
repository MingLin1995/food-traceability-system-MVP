# ========== 專案設定 ==========
APP_NAME=My Application
PROJECT_NAME=my-app    # container name
PORT=3000              # Docker 內部 port
BACKEND_PORT=3000      # 後端對外 port
FRONTEND_PORT=3001     # 前端對外 port
# 前端連線後端的 URL (瀏覽器端使用)
NEXT_PUBLIC_API_URL=http://localhost:3000

# ========== 資料庫設定 ==========
DATABASE_URL="postgresql://dev_user:dev_password@db:5432/app_dev?schema=public"
POSTGRES_USER=dev_user
POSTGRES_PASSWORD=dev_password
POSTGRES_DB=app_dev
POSTGRES_PORT=5432

# ========== JWT 設定 ==========
# 產生強密碼指令：node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# 密碼長度必須至少 32 字元，否則生產環境會拒絕啟動
JWT_SECRET=your-secret-key-change-this-in-production
JWT_EXPIRES_IN=8h

# ========== CORS 設定 ==========
# 啟用 CORS (true/false)
ENABLE_CORS=false
# 允許的來源 (多個用逗號分隔，例如: http://localhost:3000,https://example.com)
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# ========== 日誌設定 ==========
# 是否記錄所有請求（開發環境可設為 true，生產環境建議 false）
LOG_ALL_REQUESTS=false

# ========== 預設管理員帳號設定 ==========
# 這些帳號會在首次初始化資料庫時建立
ADMIN_ACCOUNT_1=admin001
ADMIN_PASSWORD_1=000000

ADMIN_ACCOUNT_2=admin002
ADMIN_PASSWORD_2=000000

ADMIN_ACCOUNT_3=admin003
ADMIN_PASSWORD_3=000000

